[server]
root_url = http://{{ hostvars[groups['hosts'][0]].ansible_host }}:{{  grafana_port_variable }}/grafana/
serve_from_sub_path = true

[security]
admin_user = {{ grafana_user }}
admin_password = {{ grafana_admin_password }}
